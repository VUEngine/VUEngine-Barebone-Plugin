VUEngine Barebone Plugin
========================

A VUEngine plugin.

![Preview Image](preview.png)


USAGE
-----

See `source/config.h` for available macros. Define the desired macro in your game's `config.h` file to override.
